# City Front 下一阶段测试记录

日期：2026-07-18

## 已完成的自动化检查

- Cocos Creator 3.8.7 TypeScript 全项目检查：通过。
- 核心战斗与存档规则：47 项通过。
- 服务端协议清洗规则：15 项通过。
- `server/index.mjs`、协议模块、健康检查和全部浏览器测试脚本语法检查：通过。
- systemd、PM2、Nginx、Caddy、Docker、日志轮转和版本化发布脚本已生成；发布脚本通过 `bash -n`。
- 构建配置仍固定为 Cocos Creator 3.8.7，程序化 PBR 材质继续使用打包资源 `procedural-standard.mtl`。

核心规则覆盖射速、独立弹量、自动与部分换弹、空弹匣、后坐力弹簧、伤害衰减、爆炸遮挡、动作互斥、重复死亡、金币、存档修复和原子重置。

服务端协议覆盖房间码、名称、阵营武器限制、队伍平衡、地下坐标、姿态、RPK/M249/PKM 弹量、最多 16 个角色、五据点、手雷和超限世界快照。

## 本阶段已修复

- 地图和任务改为开局随机，菜单不再提供城市/军事基地选择。
- 蓝红双方各提供五种任务，任务目标、任务装备和提前结束条件均进入战斗逻辑。
- 数字倒计时替换为 5 秒全屏任务简报；空降任务包含实际缓降出生，地道任务固定使用城市地下区域。
- 敌军 AI 技能比友军 AI 高一个有限档位，视距和思考频率上调，但保留个体误差。
- 单机和联机 AI 补位统一扩展为 8 对 8；服务器房间、阵营容量和权威快照同步支持 16 人。
- AI 会按距离切换主武器/Glock，低血量寻找掩体并使用医疗包，并在合适距离有限度使用手雷。
- AI 反应频率、移动、短点射、侧移、目标分流和卡住恢复已加强，同时保留技能差异避免集体精准秒杀玩家。
- 城市新增两栋可进入建筑，军事基地机库和营房改为可进入结构；建筑门洞与导航网格对齐。
- 地铁扩展为真正的地下站台、轨道和掩体区域，每方固定分配地下 AI，并将一个据点设在地下。
- 梯子扩大接近触发范围，按前进键会自动贴近、攀爬并跨上屋顶。
- HUD 始终显示弹匣、备弹、手雷和医疗包数量，并继续保留可点击的 G/H 按钮。
- 新增四倍镜、六倍镜、伸缩枪托、折叠枪托、重型枪管和加长精密枪管，并提高配件价格。
- 存档升级为版本 2，加入跨阵营共享等级、递增经验阈值、升级金币奖励和按等级武器解锁；旧存档自动迁移。
- 城市和军事基地统一为 5 个据点，城市保留地下地铁据点。
- 联机玩家地下高度不再被钳回 `y=0`。
- 统一伤害入口禁止友军伤害，投掷者仍可被自己的手雷伤害。
- 中低画质关闭实时阴影，高画质启用；低画质关闭雾效。
- 开镜时隐藏会挡住中心视线的实心镜体、镜片、机械照门和顶部导轨。
- 房间服务移除 Playwright WebSocket 回退，只允许正式 `ws` 依赖。
- 服务端增加消息频率、房间状态、武器、弹量、坐标和快照校验。
- 重复开局被拒绝；房主退出会把最后权威快照交给新房主。
- 增加 `/healthz`、优雅停止和生产安全响应头。

## 等待新构建后的运行验证

当前 `build/web-desktop` 时间为 2026-07-17 18:10，早于 2026-07-18 的源码修改，不能用于本阶段验收。新构建后必须执行：

- 1366×768、1440×900、1920×1080 三种桌面视口。
- Pointer Lock、Tab 光标模式和 Esc 暂停各连续切换 20 次。
- 两张地图、全部武器弹量、开镜无遮挡、三档画质和 20 次重开。
- 手雷烟尘、医疗中断、死亡动作阻断、复活补满和友军伤害阻断。
- 两个浏览器创建/加入房间、2 真人 + 14 AI、房主迁移和快照连续性。
- 两局完整 10 分钟比赛，总连续运行时间至少 20 分钟，并记录 FPS、JS 内存、AI 停滞和 Console 错误。

## 当前外部限制

- 正式 `ws@8.18.3` 已从本机 npm 缓存恢复并通过导入验证；公网生产发布仍需在服务器执行 `npm ci --omit=dev`。
- SSH 到 `chenyifan@175.178.194.44` 的只读连接同样被自动授权服务 503 阻断，尚未上传服务器。
- 尚未提供解析到服务器的正式域名，因此 HTTPS 证书、WSS 公网地址和跨网络双浏览器测试未完成。
- 尚未在 Windows 实体电脑和不同显卡环境验证。

以上未完成项不会被描述为已通过或已部署。
